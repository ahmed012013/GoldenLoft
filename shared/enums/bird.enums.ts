export enum BirdGender {
  MALE = "male",
  FEMALE = "female",
  UNKNOWN = "unknown",
}

export enum BirdStatus {
  HEALTHY = "healthy",
  SICK = "sick",
  UNDER_OBSERVATION = "observation",
  SOLD = "sold",
  DECEASED = "deceased",
  UNKNOWN = "unknown",
  SQUAB = "squab",
}

export enum BirdType {
  RACING = "racing",
  ORNAMENTAL = "ornamental",
  UNKNOWN = "unknown",
}
